My dotfiles, save me a few seconds when reinstalling my system.

##Installation:

    git clone https://github.com/hg8/rc.git

##Create symlinks:

    ln -s ~/rc/.vim/ ~/
    ln -s ~/rc/.vimrc ~/

##Update:

    cd ~/rc/
    git pull

